# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — ignore EVERY .env variant, keep only the committed template
.env*
!.env.example

# vercel
.vercel

# supabase CLI artifacts (never commit these — may contain refs/creds)
supabase/.temp
supabase/.branches
supabase/.env
